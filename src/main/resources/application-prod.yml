spring:
  datasource:
    url: jdbc:postgresql://<your-cloud-db>:5432/tennis # à changer vers la fin 
    driverClassName: org.postgresql.Driver
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
